import React from "react";
import Button from "../../components/common/Button";

const UserInfoNextButton = (props) => {
  return <Button {...props} />;
};

export default UserInfoNextButton;
